run:
go run .\cmd\gochi-demo\main.go

build:
cls ; go build -o .\bin\gochi-demo.exe .\cmd\gochi-demo\main.go ; .\bin\gochi-demo.exe

render build command:
go build -tags netgo -ldflags "-s -w" -o app ./cmd/gochi-demo


project-root/
├── cmd/
│   └── <app-name>/
│       └── main.go
├── internal/
│   └── <private-package-name>/
│       └── <source-files>.go
├── pkg/
│   └── <public-package-name>/
│       └── <source-files>.go
├── vendor/ (optional, for vendored dependencies)
├── api/ (optional, for API definitions like Protobuf)
├── web/ (optional, for web-related assets like templates, static files)
├── scripts/ (optional, for build, deploy, or utility scripts)
├── tests/ (optional, for integration or end-to-end tests)
├── go.mod
├── go.sum
└── README.md